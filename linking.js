const config = {
  screens: {
    Home:'home',
    Splash:'splash',
    Onboarding:'onboarding',
    Login:'login',
  },
};

const linking = {
  prefixes: ["aka://reactapp"],
  config,
};

export default linking;